def verticalDisplacement(previousVertDistance, timeStep, instantaneousVelocity):
    "finds the horizontal drift idealized with windspeed in m/s and time in s"
    return(previousVertDistance + timeStep*instantaneousVelocity)